import '../Style.css';
import React from 'react';
const About = () => {
    // const { currentUser, setCurrentUser } = useContext(UserContext);
    return(
        <p>About us</p>
    )
};
export default About;
